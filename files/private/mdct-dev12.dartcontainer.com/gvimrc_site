" Setup GVIM for this site specifically
" vim: syntax=vim


set guifont=Monospace\ 10

" Force gvimdiff to use full screen width.  I had little luck using kwin.
" Looks like kwin applies initially and then gvim forces a new geometry.
if &diff == 1
    set columns=270
    set guifont=Monospace\ 8
    set lines=78
else
    set columns=96
    set lines=31
endif

