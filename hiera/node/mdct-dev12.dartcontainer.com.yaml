# hiera/node/mdct-dev12.dartcontainer.com.yaml
#
# Synopsis:
#       These settings are specific to mdct-dev12.


---

dart::mdct_dev12::profile::root::git_proxy:   "http://%{hiera('http_proxy')}:8080"


flock_herder::conf_source:  puppet:///modules/dart/mdct-dev12/etc/herd.conf

logwatch::ignore_source:    puppet:///private-host/logwatch/ignore.conf

sendmail::enable:           true
sendmail::ensure:           running

sudo::drop_in::source:      "puppet:///modules/dart/sudo/%{::hostname}"
