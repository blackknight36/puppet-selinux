# modules/sudo/files/sudoers.mdct

# root cannot access user's home when mounted via NFS and root_squash, so
# might as well set HOME to something useful.
Defaults    always_set_home

# Allow all members of Dart's sudoers group (in LDAP) to run all commands.
%sudoers	ALL=(ALL)	ALL

# Preserve the preferred EDITOR and prophile-related variables.
Defaults    env_keep += "EDITOR VIMINIT GVIMINIT PROPHILE"
