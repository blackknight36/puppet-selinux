# root cannot access user's home when mounted via NFS, so might as well set
# HOME to something useful.
Defaults    always_set_home

# Allow all members of Dart's sudoers group (in LDAP) to run all commands.
%sudoers	ALL=(ALL)	ALL
