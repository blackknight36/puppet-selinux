# This file is managed by Puppet via the "<%= @module_name %>" module.
#
# vim: filetype=configparser

[koji]

;configuration for koji cli tool

;url of XMLRPC server
server = <%= @hub %>

;url of web interface
weburl = <%= @web %>

;url of package download site
topurl = <%= @downloads %>

;path to the koji top directory
topdir = <%= @top_dir %>

;configuration for Kerberos authentication (not used at Dart)

;the service name of the principal being used by the hub
;krbservice = host

;configuration for SSL authentication (used at Dart)
;   See /etc/pki/Koji and certgen.sh there on mdct-koji.

;client certificate
cert = ~/.koji/client.crt

;certificate of the CA that issued the client certificate
ca = ~/.koji/clientca.crt

;certificate of the CA that issued the HTTP server certificate
serverca = ~/.koji/serverca.crt

