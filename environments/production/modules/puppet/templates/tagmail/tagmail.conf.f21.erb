# For further info, see:
# http://docs.puppetlabs.com/puppet/latest/reference/config_file_tagmail.html#the-tagmailconf-file

# In this example, JF wants all tags reported, but doesn't want to bother
# everyone else with lots of noise while developing the (Koji) modules used by
# Class[mdct_koji_f21] or Class[mdct_koji_builder].
#
#all: john.florian@dart.biz
#all, !mdct_koji_f21,!mdct_koji_builder: chris.kennedy@dart.biz, collin.baker@dart.biz, levi.harper@dart.biz

# Ordinarily though, this is what we want:
all, !mdct_dev27, !mdct_dev27_kstest: <%= @puppet_admins %>

tc_util: <%= @tcutil_admins %>
