# vim: foldmethod=marker

# Resource Locations {{{1

# GIT_REPO_ORIGIN provides the Git URL for the origin of your Puppet
# resources.
GIT_REPO_ORIGIN='mdct-00fs:/home/git/puppet.git'

# ROOT provides the root path of all the puppet resources.
ROOT='/etc/puppet'

# DLO_DIR provides a path to where PuppetDB keeps the Dead Letter Office.
DLO_DIR='/var/lib/puppetdb/mq/discarded/'

# REPORT_DIR provides a path to where puppet is caching its reports.
REPORT_DIR='/var/lib/puppet/reports'

# FORGE_MODULE_PATH provides the path relative to $ROOT where you install
# modules from the Puppet Forge.
FORGE_MODULE_PATH='environments/production/forge_modules'

# RESOURCES is a space delimited list of directories and/or files under ROOT
# that are to be managed by the pp-fix-files tool.
RESOURCES=".git environments files"

# Resource Ownership and Access Controls {{{1

# RESOURCES_OWNER is the account that will own and have ultimate authority
# over all the puppet resources.  Typically this would be the root user.
RESOURCES_OWNER='root'

# RESOURCES_GROUP is the group account that will have unhindered read-access
# to all the puppet resources.  Typically the puppet master daemon would be
# a member of this group account.
RESOURCES_GROUP='puppet'

# RESOURCES_ACL_GROUP is the account that will have limited access to review
# and modify the puppet resources.  Typically administrative users would be a
# member of this group account, unless the administration is always done as
# the root user.  If RESOURCES_ACL_GROUP is set to a null string, the ACLs
# will be left unaffected.
RESOURCES_ACL_GROUP='pupadmin'

# Cleanup Parameters {{{1

# DLO_PURGE_AGE indicates how many days of age a letter must attain before it
# is to be purged from the Dead Letter Office populated by PuppetDB.  Set to
# a negative value to disable purging.
DLO_PURGE_AGE='30'

# REPORT_PURGE_AGE indicates how many days of age a report must attain before
# it is to be purged.  Set to a negative value to disable purging.
REPORT_PURGE_AGE='30'

# COMPRESS_DAYS indicates how many days of age a report must attain before it
# is to be compressed.  If COMPRESS_DAYS is greater than REPORT_PURGE_AGE,
# reports will never be compressed, only purged.
COMPRESS_DAYS='7'

# Other Preferences {{{1

# ADMIN_EMAILS is a space-delimitted list of email addresses to where any
# reports generated by pp-tools should be sent.
ADMIN_EMAILS='<%= @puppet_admins %>'

# PP_EDITOR provides name of preferred editor for puppet-tools.
PP_EDITOR="${PP_EDITOR:-${EDITOR:-vi}}"
