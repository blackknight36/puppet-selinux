# This file is managed by Puppet via the "<%= @module_name %>" module.
#
# vim: filetype=configparser

[web]
SiteName = koji
KojiTheme = <%= @theme %>

# Key urls
KojiHubURL = http://<%= @fqdn %>/kojihub
KojiFilesURL = http://<%= @fqdn %>/kojifiles

# Kerberos authentication options
# WebPrincipal = koji/web@EXAMPLE.COM
# WebKeytab = /etc/httpd.keytab
# WebCCache = /var/tmp/kojiweb.ccache
# The service name of the principal being used by the hub
# KrbService = host

# SSL authentication options
WebCert = /etc/kojiweb/kojiweb.crt
ClientCA = /etc/kojiweb/clientca.crt
KojiHubCA = /etc/kojiweb/kojihubca.crt

LoginTimeout = 72

# This must be changed and uncommented before deployment
Secret = <%= @secret %>

LibPath = /usr/share/koji-web/lib
